# GitHub Actions disabled

GitHub Actions is disabled in the repository settings. Keep this directory free
of `.yml` and `.yaml` files. The former public-surface workflow is retained only
as an inert review snapshot under `.github/retired-actions/`.
